{
    "include": [ // 자바스크립트로 컴파일하고 싶은 모든 디렉토리를 넣어준다.
        "src"
    ],
    "compilerOptions": {
        "outDir": "build", // 자바스크립트 파일이 생성될 디렉토리 지정
        "target": "ES6", // 어떤 버전의 자바스크립트로 컴파일할지 
        // https://www.typescriptlang.org/tsconfig#target
        "lib": ["ES6", "DOM"] // 작성한 코드가 어디서 실행 될 지 (DOM: 브라우저 환경에서 실행시킬 것)
    }
}